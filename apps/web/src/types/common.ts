export type Size = 'sm' | 'md' | 'lg';

export type Currency = 'USD' | 'EUR';

export type User = {
  id: number;
  name: string;
  img: string;
};

export type Duration = {
  years: number;
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
};
